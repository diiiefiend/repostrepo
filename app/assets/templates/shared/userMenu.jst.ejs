<% if(currentUser.isSignedIn()){%>
  Logged in as <a href="#users/<%= currentUser.id %>"><%= currentUser.username() %></a> - <a href="javascript: void(0);" id="logout-link">Log out</a>
<% } else { %>
  <a class="login-link" href="javascript:void(0);">Log In</a> | <a class="signup-link" href="javascript:void(0);">Sign Up</a>

  <div id="login" class="hidden">
    <div>
      <a href="javascript:void(0);" class="close">cancel</a>
      <h2>Log in</h2>
      <form>
        <label>Email</label>
        <input type="text" name="user[email]">
        <label>Password</label>
        <input type="password" name="user[password]">
        <button>Submit</button>
      </form>
    </div>
  </div>

  <div id="signup" class="hidden">
    <div>
      <a href="javascript:void(0);" class="close">cancel</a>
      <h2>Sign up</h2>
      <form>
        <label>Username</label>
        <input type="text" name="user[username]">
        <label>Email</label>
        <input type="text" name="user[email]">
        <label>Location</label>
        <input type="text" name="user[location]">
        <label>Password</label>
        <input type="password" name="user[password]">
        <button>Submit</button>
      </form>
    </div>
  </div>
<% }; %>
